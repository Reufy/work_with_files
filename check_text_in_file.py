import re


def check_name_file(len_par, f, wordDoc):
    i = 0
    while i < len_par:
        temp = wordDoc.paragraphs[i].text
        temp = temp.lower()
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        if (re.findall(r"word", temp) or re.findall(r"word", temp)) and re.findall(r"word", temp):
            i += 1
            temp_name_file = "word" + "\n"
            return temp_name_file
        i += 1